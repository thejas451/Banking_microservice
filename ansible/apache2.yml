---
- name: Install Apache2 on Webservers
  hosts: webservers
  become: yes
  tasks:
    - name: Install Apache2
      apt:
        name: apache2
        state: present
        update_cache: yes

    - name: Ensure Apache2 is running
      service:
        name: apache2
        state: started
        enabled: yes
